---
title: "MDX test"
subtitle: "Introduction of MDX test"
date: "2022-03-19T08:00:00.000Z"
categories: []
keywords: ["javascript", "es2020"]
slug: "4-new-features-in-es2020"
type: "blogPost"
featured: false
---

# Heading 1

## Heading 2

Inline `code` has `back-ticks around` it.

_emphasis text_

**bold**

**bold too**

[_some_ link](https://r3f.maximeheckel.com)

```jsx {9-10} title=Basic definition of a React Three Fiber mesh
import { Canvas } from "@react-three/fiber"
import { useRef } from "react"

const Cube = () => {
    const mesh = useRef()

    return (
        <mesh ref={mesh}>
            <boxGeometry args={[1, 1, 1]} />
            <meshBasicMaterial color={0xffffff} /> <meshBasicMaterial color={0xffffff} /> <meshBasicMaterial
                color={0xffffff}
            /> <meshBasicMaterial color={0xffffff} /> <meshBasicMaterial color={0xffffff} /> <meshBasicMaterial color={0xffffff} />
        </mesh>
    )
}

const Scene = () => {
    return (
        <Canvas>
            <Cube />
        </Canvas>
    )
}
```

1. What the hell
2. Hello

-   ashdioad
-   iqwjeiqwoe

1. First item
    - Sub-item 1
    - Sub-item 2
        1. Sub-sub-item 1
        2. Sub-sub-item 2
            - Sub-sub-sub-item 1
            - Sub-sub-sub-item 2
2. Second item Sub-item 1
    - Sub-item 1
        - Sub-sub-item 1
            1. Sub-sub-sub-item 1
            2. Sub-sub-sub-item 2
               asdasd
    - Sub-item 2
